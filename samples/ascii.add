import sys::console;

setForeground('Yellow');
println('******** THE ASCII CODE ********');
println();

print('  ');
for (i = 0; i < 16; i++)
{
	print('{0:X} ', i);
}

println();
resetColor();

for (i = 0; i < 16; i++)
{
	setForeground('Yellow');
	print('{0:X} ', i);
	resetColor();
	
	if (i is not 0 or 1 or 8 or 9)
    {
        for (j = 0; j < 16; j++)
        {
            print('{0} ', chr(16 * i + j));
        }
    }
	
	println();
}

readln();